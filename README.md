# dotfiles

My configuration files for firefox, fish, homebrew, kitty, nvim, starship.

## Installation

Clone this repo, get `stow` and run `make`. This does not install Firefox CSS, manually symlink the chrome directory.

