# dotfiles

My configuration files for firefox, fish, homebrew, kitty, nvim, starship.

## Installation

Clone this repo, get `stow` and run `make`. Run `brew bundle` in home to install useful stuff. This does not install Firefox CSS, manually copy the chrome directory (symlinking does not work for `userContent.css` on Mac).

