# dotfiles

My configuration files for firefox, fish, homebrew, kitty, nvim, starship.

## Installation

Clone this repo, get `stow` and run `make`. This does not install Firefox CSS, manually copy the chrome directory (symlinking does not work for `userContent.css` on Mac).

